{
  "name": "annotation-mcp",
  "version": "1.0.0",
  "description": "MCP server for document chunk annotation",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "annotation-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "keywords": ["mcp", "annotation", "llm"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  }
}
